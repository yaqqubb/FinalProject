@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@using IydeParfume.Areas.Admin.ViewModels.Order
@model List<ListItemViewModel>


<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Orders</h4>
        </div><!--end card-header-->
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Total</th>
                            <th>Order Status</th>
                             <th>User Email</th>
                            <th>Created at</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var slider in Model)
                        {
                            <tr>
                                <td>@slider.Id</td>
                                <td>@slider.Total</td>
                                <td>@slider.Status</td>
                                 <td>@slider.UserEmail</td>
                                <td>@slider.CreatedAt.ToString("dd/MM/yyyy")</td>
                                <td class="text-end d-flex mt-3">
                                    <a asp-route="admin-order-update" asp-route-id="@slider.Id"><i class="las la-pen text-secondary font-16"></i></a>

                                    <form method="POST" asp-route="admin-order-delete" asp-route-id="@slider.Id" id="slider-form-@slider.Id"></form>
                                    <button class="border-0" type="submit" form="slider-form-@slider.Id">
                                        <i class="las la-trash-alt text-secondary font-16"></i>
                                    </button>

                                    <a class ="btn btn-primary" asp-route="admin-orderProduct-list" asp-route-id="@slider.Id">Order Image List</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table><!--end /table-->
            </div><!--end /tableresponsive-->
        </div><!--end card-body-->
    </div><!--end card-->
</div>
